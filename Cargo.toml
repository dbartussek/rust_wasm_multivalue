[package]
name = "wasm_calling"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[workspace]
members = ["crates/*"]

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
walrus = "0.26.1"

[profile.release]
lto = true
